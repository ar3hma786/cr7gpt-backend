spring.application.name=cr7gpt

# Database connection properties
spring.datasource.url=jdbc:postgresql://localhost:5432/cr7gpt
spring.datasource.username=postgres
spring.datasource.password=8618609838

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver
spring.main.allow-bean-definition-overriding=true

#Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id={CLIENT-ID}
spring.security.oauth2.client.registration.google.client-secret={SECRET-KEY}

# OpenAI API Key
spring.ai.openai.api-key={OPENAI-API}
